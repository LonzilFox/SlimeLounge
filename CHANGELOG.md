# CHANGELOG

## v0.0.4

- 修复 `NETEASE_SEARCH_BASE=music.163.com` 导致 `Invalid URL`；自动补 `https://`，且直接指向 `music.163.com` 时改用内置搜索 API 路径。
- WebSocket 重连策略重做：显示关闭码/原因、后台暂停重复重连、返回前台立即恢复、网络 offline/online 感知、随机退避避免重连风暴。
- 心跳间隔调整为 20 秒并记录 ACK，用于区分服务在线与房间连接状态。
- 保留 v0.0.3 全部游戏、管理员、工号、排行榜和状态功能。

## v0.0.4

- 重做连接状态：区分“本地/线上服务在线”和“当前房间 WebSocket 已连接”，自动指数退避重连；本地 WebSocket 增加 TCP keepalive。
- 本地启动窗口显示 Local URL 与 LAN URL，明确本地版只在 Node 服务运行期间可用。
- 唯一 Owner 机制加强：支持私密 `OWNER_EMPLOYEE_ID` 固定 Owner；未配置时才以首个注册身份作为兼容回退。只有 Owner 能授予/撤销 Admin。
- 华为工号继续保持对外隐藏；普通新账号默认“待核验”；冲突工号不能覆盖，Owner/Admin 可核验、驳回、释放冲突并审批换工号申请。
- 管理后台新增所有已注册用户列表：在线/离线、最后在线、当前房间、个人状态、状态文字、工号核验状态、设备数。
- 新增个人状态：在线、忙碌、专注、开会、暂离、游戏中、听歌中；好友列表和管理后台同步展示。
- 网易云搜索改为 2.5 秒总超时并行尝试，缓存 5 分钟；支持可选 `NETEASE_SEARCH_BASE`，避免公司网络直连网易云时长时间卡住。
- 五子棋、中国象棋、国际象棋 AI 改为搜索型 AI；21点使用基础策略；德州加入起手牌评估、Monte Carlo 胜率估计、底池赔率与下注策略。
- 五子棋和中国象棋改为棋子落在棋盘线交叉点；修复中国象棋马腿规则。
- 所有游戏统一准备机制：所有已入座真人必须准备，AI 自动准备，才允许开始。
- 真人排位对局锁定站内页面切换；浏览器无法禁止 Alt+Tab，因此离开页面超过 2.5 秒执行服务端判罚：棋类判负、21点自动停牌、德州自动弃牌。AI 练习局不处罚。
- AI 练习继续不进入排行榜，并在 21点/德州结束后恢复真人练习前筹码。
- v0.0.2 旧棋局状态自动重置到新版准备状态，保留账号/好友/排行等长期数据。

## v0.0.2

- 修复史莱姆颜色选择与预览逻辑。
- 聊天室改为自适应多讨论区。
- 加入 Owner/Admin、音乐搜索、全游戏 AI 和排行榜。

## v0.0.1

- 全新项目 SlimeLounge。
