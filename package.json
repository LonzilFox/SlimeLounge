{
  "name": "slimelounge",
  "version": "0.0.4",
  "private": true,
  "type": "module",
  "description": "Pixel slime chat, music and multiplayer game rooms",
  "scripts": {
    "dev": "node local_server.js",
    "check": "node --check local_server.js && node --check src/index.js && node --check public/app.js && node --check shared/games.js && node tools/validate_project.js && node tools/test_games_v003.mjs",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.120.1"
  }
}
